server:
  port: ${PORT:10000} # Usa la variable de entorno PORT (que Render asigna) o 10000 si no existe

spring:
  main:
    banner-mode: "off"
  application:
    name: Lavalink

lavalink:
  server:
    password: "youshallnotpass"
    sources:
      youtube: true
      bandcamp: true
      soundcloud: true
      twitch: true
      vimeo: true
      http: true
      local: false
    bufferDurationMs: 400
    youtubePlaylistLoadLimit: 6
    playerUpdateInterval: 5
    youtubeSearchEnabled: true
    gc-warnings: true

metrics:
  prometheus:
    enabled: false

sentry:
  dsn: ""
